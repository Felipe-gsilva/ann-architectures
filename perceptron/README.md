Eu fiz o trabalho usando o `uv`, um package manager de python. No entanto, gerei o requirements.txt pra que fosse possivel rodar o main.py diretamente com o pip.

Só rode (com o venv ativado): 
```sh
pip install -r requirements.txt
```
